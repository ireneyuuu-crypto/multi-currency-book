# 旅行账本 · 部署说明

这个文件夹是一个可直接部署的 PWA(渐进式网页应用)。部署后用 iPhone 的 Safari 打开并"添加到主屏幕",就能像原生 App 一样全屏使用、离线打开。

## 目录结构

```
旅行账本-PWA/
├── index.html            入口页(加载依赖 + 应用)
├── app.jsx               应用主体(由源码 v6.jsx 自动生成,勿手改)
├── manifest.webmanifest  应用清单(名称/图标/全屏模式)
├── sw.js                 Service Worker(离线缓存)
└── icons/                应用图标(512/192/apple-touch 180)
```

## 一、本地测试（部署前可选）

不能直接双击 index.html 打开(浏览器会拦截本地文件加载 app.jsx),需要起一个本地服务器:

```bash
cd 旅行账本-PWA
python3 -m http.server 8000      # 或 npx serve .
```

浏览器打开 http://localhost:8000 即可。Service Worker 在 localhost 上可正常注册测试。

## 二、部署到 Netlify Drop(推荐,约 2 分钟)

1. 打开 https://app.netlify.com/drop
2. 把整个 `旅行账本-PWA` **文件夹**直接拖进页面(不是拖单个文件)
3. 等待几秒,得到一个 `https://xxxx.netlify.app` 网址,手机打开即可用

**强烈建议接着做这两步:**

4. **注册/登录 Netlify 并 Claim 这个站点**——否则匿名站点几天后会被回收
5. 在 Site settings → Domain management 里把随机域名改成好记的,比如 `irene-travel.netlify.app`

> ⚠️ **域名一旦定下就别换**:账目数据存在浏览器本地(IndexedDB),**和域名绑定**。换域名 = 换了存储空间,旧数据不会跟过去。同理,以后更新版本时,请在同一个站点里覆盖部署(见"更新"),**不要**重新拖一个新站点。

## 二B、部署到 Vercel(备选)

Vercel 没有拖拽上传,需要 CLI 或 Git 二选一:

**CLI 方式:**
```bash
npm i -g vercel
cd 旅行账本-PWA
vercel --prod        # 首次会引导登录;Framework 选 Other,目录用默认
```

**Git 方式:** 把文件夹推到 GitHub 仓库 → vercel.com → Add New Project → 导入该仓库 → Framework Preset 选 **Other**、Build Command 留空、Output Directory 填 `.` → Deploy。

本包全部使用**相对路径**(`./`),部署在 Netlify/Vercel 的根域名或子路径下均不会失效;应用是单页(无子路由),不存在刷新 404 的问题。

## 三、iPhone 添加到主屏幕

1. 用 **Safari** 打开部署好的网址(必须是 Safari,Chrome 上这一步不完整)
2. 点底部**分享按钮** → 向下找到**"添加到主屏幕"**
3. 确认名称"旅行账本",点添加

之后从主屏幕图标打开:全屏无地址栏、启动有图标、断网也能打开(首次联网加载过即可)。

## 四、离线能力说明

- **应用本体**:首次联网打开后全部缓存,之后飞行模式也能打开和记账
- **历史汇率**:查询过的日期会永久缓存(历史汇率不会变);没查过的日期离线时取不到 → 账目会以"汇率待补录"保存,**联网后自动补齐**
- **记账数据**:优先存在手机本地的 IndexedDB(容量大,适合含照片的数据);若浏览器不支持则自动降级 localStorage。老版本存在 localStorage 的数据会在首次打开时**自动迁移**到 IndexedDB(只补缺失,不覆盖新数据)。数据不经过任何服务器,也不会同步到其它设备
- 顶部若出现红色警告条,说明当前环境存不了数或空间满了,按提示处理

## 五、更新版本

1. 用新的 `app.jsx`(或整个文件夹)替换后,登录 Netlify → 你的站点 → **Deploys** → 把新文件夹拖进去(同一站点内覆盖部署)
2. 手机上彻底关闭 App 再打开,新版会自动生效(Service Worker 采用"联网时网络优先",无需清缓存)
3. 如果改了 `sw.js` 的缓存策略,记得把里面的 `VERSION` 号 +1

## 六、数据备份建议

数据只在你手机的浏览器里。建议:

- 定期用 App 内 `⋯ → 导出 CSV` 备份账目
- iPhone 的 iCloud 备份会包含 Safari 网站数据(含 IndexedDB),整机恢复时数据能回来
- iOS 会清理**长期不用**的网站数据;添加到主屏幕并常用即可避免,重要数据仍以 CSV 为准
- **不要**在 Safari 设置里"清除历史记录与网站数据"——这会把账目一起清掉

## 七、常见问题

| 现象 | 原因与处理 |
|---|---|
| 首次打开白屏几秒 | 正常:首次要下载 React 等依赖并转译,之后有缓存会快很多 |
| 顶部红条"数据不会保存" | 当前浏览器禁用了本地存储(如无痕模式),换普通模式打开 |
| 汇率显示"待补录" | 记账时离线或接口暂不可用,联网后自动补齐;也可在 `⋯` 菜单手动补 |
| 换了手机数据没了 | 数据不跨设备。老手机导出 CSV,新手机重新记或等后续导入功能 |
| 更新后样式错乱 | 彻底关闭 App 重开一次;仍异常则 Safari 设置里只删除该网站数据后重新打开 |

## 技术备注(给未来的自己)

- 依赖均走 CDN(unpkg / jsDelivr / tailwindcss),带固定版本号,由 SW 缓存优先托管
- `index.html` 里有一行关键垫片 `window.react = window.React`——lucide-react 的 UMD 包读取的是小写 `react` 全局,删掉会白屏
- JSX 由 @babel/standalone 在浏览器端转译(个人应用规模完全够用);若未来账目数据量大、追求启动速度,可改为本地 esbuild 预编译
- 汇率源:@fawazahmed0/currency-api(jsDelivr 主 + pages.dev 备)。**历史日期**端点内容不可变,SW 缓存优先;**@latest 实时汇率直连网络、绝不缓存**,避免拿到过期实时价
- SW 缓存只放静态资源与不可变历史汇率,**不存业务数据**;账目在 IndexedDB(键值库 tcw-db/kv)
